services:
  # The Database Service
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      # In production, we'll read these from the .env file (or .env.prod)
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - "127.0.0.1:5432:5432" # Bind to localhost only (for SSH Tunnel access)
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
      # - ./migrations:/docker-entrypoint-initdb.d # DISABLE THIS: The API now handles migrations!
    networks:
      - app_network

  # Your Rust App Service
  api:
    build: .
    restart: always
    depends_on:
      - db
    environment:
      # API expects broken down DB vars
      - DB_USERNAME=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=${POSTGRES_DB}
      - JWT_SECRET=${JWT_SECRET}
      - RUST_LOG=info
      # Binding to 0.0.0.0 is important inside the container
      - HOST=0.0.0.0
      - PORT=3000
    networks:
      - app_network
    # In production, we don't expose 3000 to the host directly; Nginx handles ingress.
    # But if you want to debug locally without Nginx, you could uncomment ports.
    # ports:
    #   - "3000:3000"

    # Nginx Reverse Proxy & SSL
  nginx:
    image: nginx:1.25-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Mount the config folder; setup.sh will swap init.conf and default.conf
      - ./nginx/conf.d:/etc/nginx/conf.d
      # Certbot webroot for the HTTP challenge
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    depends_on:
      - api
    networks:
      - app_network
    command: "/bin/sh -c 'while :; do sleep 6h & wait $$!; nginx -s reload; done & nginx -g \"daemon off;\"'"

  # Certbot for SSL
  certbot:
    image: certbot/certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    # Run certbot to renew every 12 hours
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $$!; done;'"
    networks:
      - app_network

networks:
  app_network:
    driver: bridge

volumes:
  postgres_prod_data:
